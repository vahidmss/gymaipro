/// Rule families supported by the intent intelligence engine.
enum IntentRuleType { keyword, regex, metadata }

/// Strategy label used when mapping to legacy detection results.
enum IntentIntelligenceStrategy { rule, regex, keyword, hybrid }
